apiVersion: v1
kind: Service
metadata:
  name: online-exam-frontend-service
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: online-exam-frontend