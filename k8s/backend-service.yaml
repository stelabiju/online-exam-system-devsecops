apiVersion: v1
kind: Service
metadata:
  name: online-exam-backend-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      protocol: TCP
  selector:
    app: online-exam-backend